# 基础语法
## 基础函数
### len(obj) 获取对象长度
### id(obj) 获取对象内存地址
### obj.append(value) 向obj最后添加value
### obj.copy() 复制并返回当前obj对象
### range(n) 返回从0到n的整数
### input("提示词") 从键盘获取输入
### 定义数组letters = ['a', 'b', 'c', 'd']
### 数组长度 len(letters) 